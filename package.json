{
  "name": "school-of-gains-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.js scripts/migration/verification.test.cjs"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
